CC = m68k-amigaos-gcc
RM = rm
STRIP = m68k-amigaos-strip
CFLAGS = -Os -noixemul -D__AMIGA__

.c.o:
	$(CC) $(CFLAGS) -o $@ -c $<

files = imploder.o

all: imploder

imploder: $(files)
	$(CC) $(CFLAGS) -o $@ $(files) $(LIBS)
	$(STRIP) $@

clean:
	-$(RM) $(files)
